<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="treasureHunt"
            type="com.orienteer.models.TreasureHunt" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_gravity="center"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_margin="8dp"
        app:cardCornerRadius="4dp">

        <TextView
            android:id="@+id/info_text"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="8dp"
            android:text="@{treasureHunt.name}"
            android:textAlignment="center"
            tools:text="This is some text!" />

        <TextView
            android:id="@+id/lat_lng_text"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@{treasureHunt.location.toString()}"
            android:paddingTop="24dp"
            android:textAlignment="center"
            tools:text="42.689, 122.3038" />

        <ImageView
            android:id="@+id/imageView2"
            android:layout_width="match_parent"
            android:layout_height="154dp"
            android:layout_gravity="bottom"
            app:srcCompat="@drawable/pirate_map" />


    </androidx.cardview.widget.CardView>
</layout>